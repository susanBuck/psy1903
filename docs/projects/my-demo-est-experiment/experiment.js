for (let block of conditions) {
    console.log(block);

    // Screen to introduce the block

    for (let trial of block.trials) {
        console.log(trial);

        // Screen to display the word as the selected color
        // Listen r,g,b keys
        // on_finish - compare data.response against trial.expectedResponse
        // data.correct
    }
}